<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>



    <!-- 引入外部配置文件 -->
    <properties resource="mysql.properties"></properties>

    <!--&lt;!&ndash;&lt;!&ndash; 为JAVA Bean起类别名 &ndash;&gt;&ndash;&gt;-->
    <!--<typeAliases >-->
             <!--&lt;!&ndash; 别名方式1，一个一个的配置 type中放置的是类的全路径，alias中放置的是类别名-->
         <!--<typeAliase type="com.cy.mybatis.beans.UserBean" alias="UserBean"/> &ndash;&gt;-->
            <!--&lt;!&ndash; 别名方式2，自动扫描，将JAVA类的类名作为类的类别名 &ndash;&gt;-->
            <!--<package name="com.mybatis.mapper"/>-->
            <!--<package name="com.mybatis.domain"/>-->
    <!--</typeAliases>-->

    <settings>
        <!-- Globally enables or disables any caches configured in any mapper under this configuration -->
        <setting name="cacheEnabled" value="false"/>
        <!-- Sets the number of seconds the driver will wait for a response from the database -->
        <setting name="defaultStatementTimeout" value="5"/>
        <!-- Enables automatic mapping from classic database column names A_COLUMN to camel case classic Java property names aColumn -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!-- Allows JDBC support for generated keys. A compatible driver is required.
        This setting forces generated keys to be used if set to true,
         as some drivers deny compatibility but still work -->
        <setting name="useGeneratedKeys" value="true"/>
    </settings>



    <!-- Continue editing here -->
    <!--配置mybatis的环境-->
    <environments default="mysql">

        <environment id="mysql">
            <!--配置事务管理器,type=jdbc代表使用jdbc的提交和回滚-->
            <transactionManager type="JDBC"></transactionManager>
            <!-- mybatis提供了3种数据源类型，分别是：POOLED,UNPOOLED,JNDI -->
            <!-- POOLED 表示支持JDBC数据源连接池 -->
            <!-- UNPOOLED 表示不支持数据源连接池 -->
            <!-- JNDI 表示支持外部数据源连接池 -->
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}" />
                <property name="url" value="${jdbc.url}" />
                <property name="username" value="${jdbc.username}" />
                <property name="password" value="${jdbc.password}" />
            </dataSource>
        </environment>


    </environments>

    <mappers>

        <!--<mapper resource="src/main/java/com/mybatis/mapper/UserMapper.xml" />-->

        <package  name="com.mybatis.mapper"  />
        <!--<mapper url="com/mybatis/mapper/userMapper.xml" />-->

    </mappers>


</configuration>